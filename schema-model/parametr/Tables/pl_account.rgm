table "parametr"."pl_account" {
  primaryKey = {
    columns = [
      "parametr_id"
    ]
    name = "pk_pl_account"
    objectType = primaryKey
  }
  owner = postgres
  columns = (
    column "parametr_id" {
      type = integer
      null = NOT NULL
    }
  )
}

foreignKey "parametr"."pl_account"."fk_pl_account_list" {
  columns = [
    "parametr_id"
  ]
  referencedTable = "parametr"."list"
  referencedColumns = [
    "id"
  ]
  onUpdate = CASCADE
  onDelete = CASCADE
}

